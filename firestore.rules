rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for debugging - REMOVE THIS IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
